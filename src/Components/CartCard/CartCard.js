import React from 'react';
import './CartCard.css';

const CartCard = (props) => {
    console.log(props);
    return (
        <div>
            <h2>Hello From Cart Card</h2>
        </div>
    );
};

export default CartCard;